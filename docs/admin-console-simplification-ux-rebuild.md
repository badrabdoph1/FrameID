# Admin Console Simplification & UX Rebuild

## الهدف

إعادة تنظيم تجربة لوحة الإدارة الرئيسية لتكون مناسبة للاستخدام اليومي من مالك المنصة، بدون حذف أي نظام داخلي أو كسر البنية المعمارية الحالية.

الفلسفة الجديدة:

- لا تظهر كل أدوات Enterprise في الواجهة اليومية.
- Dashboard هو مركز القيادة الحقيقي.
- Sidebar يحتوي على مراكز قليلة وواضحة.
- الأنظمة المتقدمة محفوظة داخل النظام أو البحث ولا تشوش الاستخدام اليومي.
- كل Workspace يعرض ملخصًا وروابط مهمة بدل إجبار المستخدم على فتح صفحات كثيرة.

## ما تم تبسيطه

### قبل التعديل

كان الـ Sidebar يعرض مجموعات كثيرة:

- الرئيسية
- الإدارة
- المحتوى
- التشغيل
- النظام
- الإعدادات

وبداخلها روابط كثيرة مثل Operations وJobs وEmail وSecurity وFeature Flags وغيرها. هذه البنية قوية لكنها مزدحمة للاستخدام اليومي.

### بعد التعديل

تم تبسيط الواجهة اليومية إلى 5 مراكز فقط:

1. القيادة
2. العملاء
3. المال
4. المحتوى
5. النظام

كل مركز يمثل مهمة يومية واضحة.

## ما تم دمجه

### 1. المال

تم إنشاء Workspace جديد:

`/admin/billing`

يجمع:

- المدفوعات المعلقة.
- آخر طلبات الدفع.
- الاشتراكات القريبة من الانتهاء.
- الباقات.
- إعدادات الدفع.
- ملخص الإيراد الشهري.

الصفحات القديمة لم تُحذف:

- `/admin/payments`
- `/admin/subscriptions`
- `/admin/plans`
- `/admin/settings/payment`

لكن أصبحت مرتبطة من Workspace المال بدل الظهور كأقسام منفصلة كثيرة.

### 2. النظام

تم إنشاء Workspace جديد:

`/admin/system`

يجمع:

- الأخطاء.
- الإشعارات.
- النسخ الاحتياطي.
- السجلات.
- إعدادات المنصة.
- الأدوات المتقدمة.

الأدوات المتقدمة ما زالت موجودة:

- Feature Flags
- Jobs Queue
- Email Center
- Security
- Admin Users
- Support
- Audit

لكن تم نقلها لجزء متقدم داخل النظام.

### 3. المحتوى

تم تحويل `/admin/content` من Content Studio فقط إلى Content Workspace يجمع:

- القوالب.
- الثيمات.
- الوسائط.
- محتوى الموقع التسويقي والقانوني.
- أحدث الوسائط.

الصفحات القديمة محفوظة:

- `/admin/templates`
- `/admin/themes`
- `/admin/media`
- `/admin/content/...`

### 4. القيادة

تمت إعادة بناء `/admin` لتكون مركز قيادة يومي.

تعرض الآن:

- أهم إجراء يحتاج تدخل الآن.
- المدفوعات المعلقة.
- التجارب التي تنتهي قريبًا.
- آخر العملاء.
- أخطاء النظام.
- الإشعارات.
- حالة النسخ الاحتياطي.
- حالة المنصة.
- أزرار إجراءات سريعة.

## ما تم إخفاؤه من الواجهة اليومية

لم يتم حذف أي نظام، لكن تم إبعاد الأدوات غير اليومية عن الواجهة الأساسية:

- Operations
- Jobs Queue
- Email Center
- Security
- Admin Users
- Feature Flags
- Support
- Analytics
- Health

هذه الأدوات محفوظة في `advancedAdminLinks` وتظهر من Workspace النظام أو البحث عند الحاجة.

## لماذا هذا التنظيم أفضل

### القيادة

بدل فتح عدة صفحات لمعرفة ما يحدث، Dashboard تعرض أهم الأمور فورًا.

### العملاء

لأن الإدارة اليومية غالبًا تبدأ من العميل: معرفة حالته، موقعه، اشتراكه، مدفوعاته، وآخر نشاطه.

### المال

لأن التفعيل والدفع مرتبطان جدًا: لا يجب فصل Payment Requests عن Subscriptions وPlans وPayment Settings في الاستخدام اليومي.

### المحتوى

لأن القوالب والثيمات والوسائط والمحتوى كلها أجزاء من تجربة الموقع، ومن الأفضل وجودها في مركز واحد.

### النظام

لأن الأخطاء والنسخ والإشعارات والسجلات أدوات مراقبة وصيانة، وليست مهام بيع أو إدارة عميل مباشرة.

## ما لم يتم حذفه

تم الحفاظ على:

- RBAC
- Audit Logs
- Feature Flags
- Jobs
- Email Center
- Security
- Admin Users
- Search
- Analytics
- Health
- Support
- Backups

أي أن التبسيط تم على تجربة الاستخدام وليس على المعمارية.

## Mobile First

- الـ Sidebar أصبح أقل ازدحامًا.
- Mobile Nav يعتمد على أهم المراكز فقط.
- Dashboard وWorkspaces الجديدة مبنية بالكروت والـ Cards بدل الجداول الكبيرة.
- المراكز الجديدة تقدم روابط وإجراءات واضحة مناسبة للهاتف.

## قابلية التوسع

التنظيم الجديد يسمح بإضافة مستقبلية مثل:

- Analytics
- API Center
- Domains
- Security
- Roles

بدون إعادة بناء اللوحة؛ يتم إضافتها كروابط متقدمة أو مراكز جديدة عند الحاجة.

## الملفات التي تم تعديلها أو إضافتها

- `src/modules/admin/navigation.ts`
- `src/app/(admin)/admin/page.tsx`
- `src/app/(admin)/admin/billing/page.tsx`
- `src/app/(admin)/admin/system/page.tsx`
- `src/app/(admin)/admin/content/page.tsx`
- `docs/admin-console-simplification-ux-rebuild.md`

## ملاحظات التحقق

تم فحص التعديلات يدويًا من ناحية:

- تقليل مجموعات Sidebar.
- عدم حذف الأنظمة الداخلية.
- عدم كسر الروابط القديمة.
- إبقاء الأدوات المتقدمة قابلة للوصول.
- إزالة رابط مكرر في قسم المحتوى.
- استخدام حقول Prisma صحيحة مثل `storageKey` بدل `fileName` في MediaAsset.

لم يتم تشغيل Build أو TypeScript أو Tests داخل هذه البيئة لعدم وجود Runtime/CI مباشر هنا. يجب تشغيلها في Railway أو بيئة التطوير، وأي خطأ يظهر يتم إصلاحه من اللوج مباشرة.
